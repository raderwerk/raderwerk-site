# CLAUDE.md

Zie `AGENTS.md`.

Dezelfde regels gelden onverkort voor Claude in deze repo: scope beperkt tot deze site, de Definition of Done uit `AGENTS.md`, dezelfde PR-conventies, en dezelfde verboden handelingen — geen merge, geen force-push, geen deploy, geen geheimen. Draai `npm run check` en `npm run build` altijd vóór het openen van een pull request. Sluit elke bijdrage af met de rol die het werk deed.
