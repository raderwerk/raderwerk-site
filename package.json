{
  "name": "raderwerk-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "measure:lcp": "node scripts/measure-lcp.mjs",
    "astro": "astro",
    "test": "npm run build && node --test scripts/check-site.test.mjs && node scripts/check-site.mjs"
  },
  "dependencies": {
    "astro": "^7.2.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "playwright": "^1.55.0",
    "typescript": "^5.7.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}
